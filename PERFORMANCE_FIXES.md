# PERFORMANCE_FIXES — 性能与高并发优化记录

## 1. 异步非阻塞与事件循环优化
- **异步密码学哈希计算**：将 `users.js`、`auth.js` 以及 `shares.js` 中原先基于同步 `crypto.pbkdf2Sync` / `crypto.scryptSync` 的密集 CPU 计算全面升级为 Promise 化非阻塞异步实现 (`crypto.pbkdf2` / `crypto.scrypt`)，彻底消除登录、密码校验及分享鉴权时的 Node.js 单线程事件循环阻塞。
- **高并发并发锁控制**：引入基于细粒度资源的内存并发排队锁，防止同文件并发写或短时间内高频保存导致的争用与脏数据。

## 2. 内存与缓存索引优化
- **多数据库引擎缓存对齐**：为 JSON 文件引擎、SQLite、PostgreSQL 和 MySQL 统一构建内存级二级只读元数据缓存与 LRU 索引，避免每次读取或列出笔记都产生全量磁盘 I/O。
- **大文件流式传输**：附件上传与下载改为流式传输 (`stream.pipeline` / `fs.createReadStream`)，降低单次传输数以兆计媒体文件时的内存常驻峰值。

## 3. WebSocket 广播防抖与过滤
- **增量变更防抖广播**：对编辑器高频输入的微小变更实行防抖（Debounce）合并广播，避免网络套接字消息风暴。
- **客户端排除机制**：广播变更时自动过滤触发当前变更的源设备连接（Echo 抑制），节约客户端重复渲染开销与多余网络流量。
